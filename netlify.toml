[build]
  command = "npm run build"
  publish = "dist"

# Redirects for single-page application
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Enable Netlify forms
[build.processing.html]
  pretty_urls = true 